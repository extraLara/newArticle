{% extends 'base.html.twig' %}

{% block title %}Editer un article{% endblock %}

{% block body %}

    <div class="py-5">
        <div class="container">
            <div class="row">
                <div class="col-md-8 text-left">
                    <h1 class="text-left" style="color:#68b0ab">Editer un article</h1>
                </div>
                <div class="col-md-2 text-left">
                    <a href="{{ path('article_index') }}" class="btn btn-primary">Retour à la liste</a>
                </div>
                <div class="col-md-2 text-left">
                    {{ include('article/_delete_form.html.twig') }}
                </div>
            </div>
            <div class="row">
                <div class="col-md-12 text-left">
                    {{ include('article/_form.html.twig', {'button_label': 'Editer l\'article'}) }}
                </div>
            </div>
            <div class="row">
                <div class="col-md-6">
                </div>
                <div class="col-md-6">
                    
                </div>
            </div>
        </div>
    </div>

{% endblock %}
